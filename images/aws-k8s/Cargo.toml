[package]
name = "aws-k8s"
version = "0.1.0"
edition = "2018"
publish = false
build = "build.rs"

[package.metadata.build-image]
included-packages = [
    "aws-iam-authenticator",
    "cni",
    "cni-plugins",
    "kubelet",
    "release",
]

[lib]
path = "img.rs"
